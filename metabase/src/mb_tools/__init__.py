"""Orchestration for this repo's Metabase instance: audit it, build the
transforms the manifest declares, export content to git. Not a place for
Metabase know-how — that ships with mb-cli, versioned against the binary."""
